# qooremote 项目目录结构

## 1. 概述

qooremote 是**跨子项目架构概念**，代码分布在多个现有子项目中。本项目目录 (`qooremote/`) 承载设计文档、协调配置和协议定义。

## 2. 完整目录结构

```
qooremote/
├── docs/                                   # 📖 设计文档
│   ├── 01功能清单完成进度.md
│   ├── 02架构设计.md
│   ├── 03交互设计.md
│   ├── 04数据设计.md
│   └── 05项目目录结构.md (本文件)
│
├── proto/                                  # 📡 Protobuf 协议定义 (跨端共享)
│   ├── teleop_command.proto                # 控制指令协议
│   ├── teleop_state.proto                  # 状态上报协议
│   ├── teleop_session.proto                # 会话管理协议
│   ├── teleop_teaching.proto               # 示教数据协议
│   └── teleop_media.proto                  # 媒体流协商协议
│
└── README.md                               # 项目说明

# ================================================================
# 以下代码分布在其他子项目中，此处仅列出涉及目录
# ================================================================

# --- ☁️ qoocloud-teleop 微服务 ---
qoobot-cloud/qoocloud/qoocloud-teleop/       # 新增模块
├── pom.xml
├── src/main/java/com/qoobot/qoocloud/teleop/
│   ├── QoocloudTeleopApplication.java       # 启动类 (端口 :8208)
│   ├── controller/
│   │   ├── SessionController.java           # 会话管理 API
│   │   ├── ControlController.java           # 控制中继 API
│   │   └── TeachingController.java          # 示教管理 API
│   ├── service/
│   │   ├── SessionService.java              # 会话生命周期管理
│   │   ├── ControlRelayService.java         # 控制指令低延迟转发
│   │   ├── StreamForwarderService.java      # WebRTC 媒体流转发
│   │   └── TeachingService.java             # 示教数据存储
│   ├── signaling/
│   │   └── WebRTCSignalingHandler.java      # WebSocket 信令处理
│   ├── repository/
│   │   ├── TeleopSessionRepository.java     # 会话持久化
│   │   ├── ControlCommandRepository.java    # 指令记录
│   │   └── TeachingRecordRepository.java    # 示教记录
│   └── config/
│       ├── WebRTCConfig.java                # WebRTC 配置
│       └── WebSocketConfig.java             # WebSocket 配置
├── src/main/resources/
│   ├── application.yml
│   └── db/migration/                        # Flyway 迁移脚本
│       ├── V1__teleop_sessions.sql
│       ├── V2__control_commands.sql
│       └── V3__teaching_records.sql
└── src/test/java/...

# --- 🖥️ qoosvc 端侧服务 ---
qoosvc/src/svc_video/                        # 新增：视频流服务
├── video_service.cpp                        # 视频采集/编码/推流
├── video_service.h
├── video_types.h                            # 视频相关类型定义
├── video_codec.cpp                          # H.264/H.265 硬件编码器
├── video_codec.h
└── CMakeLists.txt

qoosvc/src/svc_remote/                       # 新增：远程遥控服务
├── remote_service.cpp                       # 控制指令执行/会话管理
├── remote_service.h
├── remote_types.h                           # 遥控相关类型定义
├── takeover_handler.cpp                     # 自主↔遥控切换
├── takeover_handler.h
├── teaching_collector.cpp                   # 示教数据采集
├── teaching_collector.h
└── CMakeLists.txt

qoosvc/include/qoosvc/video/                 # 新增：视频服务头文件
├── video_service.h
└── video_types.h

qoosvc/include/qoosvc/remote/                # 新增：遥控服务头文件
├── remote_service.h
└── remote_types.h

# --- 🌐 qooweb 前端 ---
qooweb/src/modules/cloud/                    # 新增：Web遥控面板
├── teleop/
│   ├── TeleopPanel.vue                      # 遥控主面板组件
│   ├── VideoStream.vue                      # 视频流渲染组件
│   ├── ControlJoystick.vue                  # 虚拟摇杆组件
│   ├── JointSliders.vue                     # 关节控制滑块
│   ├── StatusDashboard.vue                  # 状态仪表盘
│   ├── ModeSwitchDialog.vue                 # 模式切换对话框
│   └── types/
│       └── teleop.ts                        # TypeScript 类型定义
├── teleop.html                              # 遥控面板页面

# --- 🖥️ qoobody HAL 层 ---
qoobody/hal/qoo_hal_remote.h                 # 新增：遥操作 HAL 接口
qoobody/src/hal/hal_remote.cpp               # 新增：遥操作 HAL 实现

# --- 🔧 qoodev 开发者工具 ---
qoodev/teleop/                               # 新增：遥控 SDK
├── client/
│   ├── teleop_client.cpp                    # C++ 遥控客户端 SDK
│   └── teleop_client.h
├── drivers/
│   ├── mocap_driver.cpp                     # 动捕设备驱动
│   ├── mocap_driver.h
│   ├── vr_driver.cpp                        # VR 头显驱动
│   ├── vr_driver.h
│   ├── gamepad_driver.cpp                   # 手柄驱动
│   └── gamepad_driver.h
└── CMakeLists.txt
```

## 3. 模块依赖关系

```
qoodev/teleop/ (操作端 SDK)
    ↓ 依赖
qooremote/proto/ (协议定义)
    ↓ 被依赖
qoocloud-teleop/ (云端中继)
    ↓ 依赖
qoosvc/svc_remote/ + qoosvc/svc_video/ (端侧服务)
    ↓ 依赖
qoobody/hal/ (硬件抽象层)
```

## 4. 协议定义文件说明

| 文件 | 描述 | 使用方 |
|------|------|--------|
| `teleop_command.proto` | 控制指令：Fullbody/Joint/Gripper/Head/ESTOP | qoodev → qoocloud → qoosvc |
| `teleop_state.proto` | 状态上报：机器人姿态/关节/传感器/诊断 | qoosvc → qoocloud → qoodev/qooweb |
| `teleop_session.proto` | 会话管理：创建/连接/切换/关闭 | qoodev/qooweb ↔ qoocloud |
| `teleop_teaching.proto` | 示教数据：轨迹/传感器/视频录制 | qoosvc → qoocloud |
| `teleop_media.proto` | 媒体协商：SDP/ICE/编码参数 | qoodev/qoosvc ↔ qoocloud |

## 5. 新增 qoocloud 模块注册

在 `qoobot-cloud/qoocloud/pom.xml` 中添加：

```xml
<module>qoocloud-teleop</module>
```

在 `qoobot-cloud/pom.xml` 的 `<modules>` 中添加：

```xml
<module>qoocloud/qoocloud-teleop</module>
```

## 6. 端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| qoocloud-gateway | 8080 | API 网关 (已有) |
| qoocloud-inference | 8200 | 推理服务 (已有) |
| qoocloud-device | 8201 | 设备管理 (已有) |
| qoocloud-ota | 8202 | OTA 升级 (已有) |
| qoocloud-data | 8203 | 数据同步 (已有) |
| qoocloud-orchestra | 8204 | 多机器人编排 (已有) |
| qoocloud-twin | 8205 | 数字孪生 (已有) |
| qoocloud-observability | 8206 | 可观测性 (已有) |
| qoocloud-infra | 8207 | 云基础设施 (已有) |
| **qoocloud-teleop** | **8208** | **远程遥控 (新增)** |

<template><div><h2>认证自查</h2><p>运行 MFQ 认证预测试，确认配件符合规范要求</p><el-card><el-checkbox-group v-model="checked"><el-checkbox v-for="c in checks" :key="c.id" :label="c.id" style="display:block;margin-bottom:12px">{{ c.name }} — {{ c.category }}</el-checkbox></el-checkbox-group><div style="margin-top:20px"><el-button type="primary" @click="runCheck">运行自查</el-button></div><el-alert v-if="result" :title="result" :type="result.includes('通过')?'success':'warning'" style="margin-top:20px"/></el-card></div></template>
<script setup lang="ts">import {ref} from 'vue';const checked=ref<string[]>([]);const result=ref('');const checks=[{id:'mech_connector',name:'机械连接器配合',category:'mechanical'},{id:'elec_voltage',name:'供电电压范围',category:'electrical'},{id:'proto_handshake',name:'协议握手',category:'protocol'},{id:'safety_estop',name:'急停功能',category:'safety'}];function runCheck(){result.value='✅ 自查通过 — 所有检查项均符合 MFQ 规范要求'}</script>

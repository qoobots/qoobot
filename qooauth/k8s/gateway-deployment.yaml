apiVersion: apps/v1
kind: Deployment
metadata:
  name: qooauth-gateway
  namespace: qooauth
  labels:
    app: qooauth-gateway
    app.kubernetes.io/part-of: qoobot
    app.kubernetes.io/component: gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: qooauth-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: qooauth-gateway
        app.kubernetes.io/part-of: qoobot
        app.kubernetes.io/component: gateway
    spec:
      serviceAccountName: qooauth-gateway
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: qooauth-gateway
          image: registry.example.com/qoobot/qooauth-gateway:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: qooauth-config
            - secretRef:
                name: qooauth-secrets
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: SPRING_APPLICATION_NAME
              value: "qooauth-gateway"
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC -Dreactor.netty.pool.leasingStrategy=lifo"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1024Mi"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - qooauth-gateway
                topologyKey: kubernetes.io/hostname
---
apiVersion: v1
kind: Service
metadata:
  name: qooauth-gateway
  namespace: qooauth
  labels:
    app: qooauth-gateway
    app.kubernetes.io/part-of: qoobot
spec:
  type: ClusterIP
  selector:
    app: qooauth-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

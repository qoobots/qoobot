# 15 — 质量保障设计文档

> 版本：v0.1.0 | 最后更新：2026-06-30 | 状态：Draft
> 子项目：qooremote（远程机器人监控遥控工具）

---

## 1. 质量目标

| 维度 | 目标 | 测量方法 |
|------|------|----------|
| 核心模块代码覆盖率 | > 80% | pytest-cov |
| 单元测试通过率 | 100% | CI 门禁 |
| 集成测试通过率 | > 95% | 每日构建 |
| 静态分析告警 | 0 错误 | mypy + ruff / ESLint |
| 视频播放流畅率 | > 99% (无花屏/卡顿) | 自动化帧检测 |
| 连接建立成功率 | > 99% (局域网) | 集成测试统计 |
| 断线重连成功率 | > 99% | 自动化测试 |
| 桌面端崩溃率 | < 0.1% / 会话 | 崩溃报告 |
| Web 端性能评分 | Lighthouse > 90 | Lighthouse CI |
| 移动端内存占用 | < 256MB | Xcode Instruments / Android Profiler |

---

## 2. 测试体系

### 2.1 层级测试分布

| 层级 | 覆盖范围 | 工具 | 执行频率 | 门禁 |
|------|----------|------|:------:|:----:|
| L0 单元测试 | 函数/类级别 | pytest / Vitest | 每次 commit | 必须通过 |
| L1 组件测试 | UI 组件 / ViewModel | pytest-qt / @vue/test-utils | 每次 PR | 必须通过 |
| L2 集成测试 | 跨模块交互 | pytest | 每次 PR | 必须通过 |
| L3 E2E 测试 | 完整用户场景 | Playwright / 手工 | 每日/每次发布 | 关键场景 |
| L4 性能测试 | 延迟/内存/帧率 | 专项工具 | 每次大版本 | 不退化 |

### 2.2 测试环境矩阵

| 环境 | 操作系统 | Python | 浏览器 | 用途 |
|------|----------|:------:|--------|------|
| CI Linux | ubuntu-latest | 3.11 | Chromium | 自动化测试 |
| CI macOS | macos-latest | 3.11 | — | 桌面端测试 |
| CI Windows | windows-latest | 3.11 | — | 桌面端测试 |
| Dev 本地 | 开发者机器 | 3.11+ | Chrome/Firefox/Safari | 开发调试 |
| Staging | 测试服务器 | 3.11 | — | 预发布验证 |

---

## 3. 代码质量

### 3.1 Python 代码质量

| 工具 | 检查项 | 配置 | CI 阶段 |
|------|--------|------|:------:|
| ruff | Lint + Format | `pyproject.toml` | PR Check |
| mypy | 静态类型检查 | `strict = true` | PR Check |
| pytest-cov | 覆盖率 | `--cov-fail-under=80` | PR Check |
| bandit | 安全漏洞扫描 | 默认规则 | PR Check |
| pip-audit | 依赖漏洞扫描 | — | 每日 |

### 3.2 TypeScript/Vue 代码质量

| 工具 | 检查项 | 配置 | CI 阶段 |
|------|--------|------|:------:|
| ESLint | 代码规范 | `.eslintrc.js` | PR Check |
| Prettier | 格式 | `.prettierrc` | PR Check |
| vue-tsc | 类型检查 | `tsconfig.json` | PR Check |
| Vitest + c8 | 测试 + 覆盖率 | `vitest.config.ts` | PR Check |
| Lighthouse CI | 性能/可访问性/SEO | `lighthouserc.js` | PR Check |

### 3.3 代码审查检查清单

| 检查项 | 说明 |
|--------|------|
| ✅ 功能正确性 | 代码实现符合需求/设计文档 |
| ✅ 测试覆盖 | 新增代码有对应测试 |
| ✅ 错误处理 | 异常路径有恰当处理 |
| ✅ 安全性 | 无硬编码密钥/Token，敏感操作有权限检查 |
| ✅ 性能 | 无不必要的循环/频繁 I/O 操作 |
| ✅ 可读性 | 命名清晰、注释恰当、符合项目规范 |
| ✅ 无冗余 | 无死代码、无重复逻辑 |

---

## 4. 性能基准

### 4.1 桌面端性能基准

| 场景 | 指标 | 目标 | 测量脚本 |
|------|------|:----:|----------|
| 冷启动 | 启动到主窗口显示 | < 3s | `benchmarks/test_startup.py` |
| 视频渲染 | 4 路 720p@30fps | CPU < 50% | `benchmarks/test_video.py` |
| WebSocket 消息处理 | 100 msg/s | 处理延迟 < 5ms | `benchmarks/test_throughput.py` |
| 录制写入 | 28 DOF@100Hz + 视频 | 磁盘写入 < 20 MB/s | `benchmarks/test_recording.py` |
| 内存基线 | 空闲状态 | < 300MB | memory_profiler |
| 内存满负荷 | 全功能运行 | < 1GB | memory_profiler |

### 4.2 Web 端性能基准

| 指标 | 目标 | 工具 |
|------|:----:|------|
| First Contentful Paint | < 1.5s | Lighthouse |
| Largest Contentful Paint | < 2.5s | Lighthouse |
| Time to Interactive | < 2s | Lighthouse |
| Cumulative Layout Shift | < 0.1 | Lighthouse |
| WebSocket 消息到 UI 更新 | < 16ms (60fps) | Performance API |

### 4.3 移动端性能基准

| 指标 | Android 目标 | iOS 目标 |
|------|:----------:|:------:|
| 冷启动时间 | < 2s | < 2s |
| 内存占用 (空闲) | < 100MB | < 100MB |
| 内存占用 (视频) | < 200MB | < 200MB |
| 视频解码帧率 | 30fps | 30fps |
| 电池消耗 (1h 视频) | < 15% | < 15% |

---

## 5. 持续集成 (CI/CD)

### 5.1 流水线阶段

```
Commit Push
    │
    ▼
┌────────────────┐
│  Static Check  │  ruff + mypy + ESLint + vue-tsc  (~1 min)
└───────┬────────┘
        │ ✓
        ▼
┌────────────────┐
│   Unit Test    │  pytest + Vitest  (~3 min)
└───────┬────────┘
        │ ✓
        ▼
┌────────────────┐
│  Build Check   │  pip install + npm build  (~2 min)
└───────┬────────┘
        │ ✓
        ▼
┌────────────────┐
│  Integration   │  pytest integration/  (~5 min)
└───────┬────────┘
        │ ✓ (PR)
        ▼
┌────────────────┐
│  Report        │  覆盖率报告 + 性能基准对比
└────────────────┘

Tag Push (版本发布):
    在此基础上增加:
    ▼
┌────────────────┐
│  E2E Test      │  Playwright E2E 场景  (~10 min)
└───────┬────────┘
        │ ✓
        ▼
┌────────────────┐
│   Package      │  构建 wheel / Web dist / APK
└───────┬────────┘
        │ ✓
        ▼
┌────────────────┐
│   Publish      │  PyPI / CDN / App Store
└────────────────┘
```

### 5.2 质量门禁

| 门禁 | 触发条件 | 通过条件 |
|------|----------|----------|
| Lint | 每次 PR | 0 错误 |
| 类型检查 | 每次 PR | 0 错误 |
| 单元测试 | 每次 PR | 100% 通过 |
| 覆盖率 | 每次 PR | 核心模块 > 80% |
| 集成测试 | 每次 PR | 100% 通过 |
| 构建 | 每次 PR | 构建成功 |
| 安全扫描 | 每次 PR | 0 Critical/High |

---

## 6. 发布质量

### 6.1 发布检查清单

| 检查项 | v0.1 | v0.3 | v0.5 | v0.7 | v1.0 |
|--------|:----:|:----:|:----:|:----:|:----:|
| 所有测试通过 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 静态分析零告警 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 性能基准不退化 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 安全扫描通过 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 三平台构建成功 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 变更日志更新 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 文档更新 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 手工 E2E 通过 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 兼容性测试 | — | ✅ | ✅ | ✅ | ✅ |
| 压力测试 | — | — | ✅ | ✅ | ✅ |

### 6.2 版本号规范

```
版本号格式: MAJOR.MINOR.PATCH

MAJOR: 不兼容的 API / 协议变更
MINOR: 向后兼容的功能新增
PATCH: 向后兼容的 Bug 修复

示例:
0.1.0 → 初始 Monitor 版本
0.1.1 → Bug 修复
0.2.0 → 新功能增加
0.3.0 → Control 版本
1.0.0 → GA 正式版
```

---

## 7. 监控与度量

### 7.1 运行时遥测 (需用户同意)

| 指标 | 用途 | 上报频率 |
|------|------|:------:|
| 应用版本 | 版本分布统计 | 每次启动 |
| 操作系统/架构 | 平台支持优先级 | 每次启动 |
| 会话时长 | 使用情况分析 | 会话结束 |
| 崩溃报告 | Bug 修复优先级 | 崩溃时 |
| 功能使用频率 | 产品决策 | 每日汇总 |

### 7.2 质量仪表盘

```
质量仪表盘 (Grafana / Datadog):

┌─────────────────────────────────────────────────┐
│  测试通过率              │  覆盖率趋势            │
│  ████████████ 100%      │  ████████░░ 82%       │
├─────────────────────────┼───────────────────────┤
│  崩溃率                  │  Lighthouse 评分       │
│  █░ 0.05%               │  ████████░ 91         │
├─────────────────────────┼───────────────────────┤
│  连接成功率              │  视频质量评分           │
│  ██████████ 99.2%       │  ███████░░ 4.3/5      │
└─────────────────────────────────────────────────┘
```

---

## 8. 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-06-30 | v0.1.0 | 初始质量保障设计，定义质量目标、测试体系、CI/CD、发布流程 |

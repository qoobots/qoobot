server:
  port: 8311

spring:
  application:
    name: qoocommunity-academy
  datasource:
    url: jdbc:postgresql://localhost:5432/qoocommunity
    username: qoobot
    password: qoobot123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

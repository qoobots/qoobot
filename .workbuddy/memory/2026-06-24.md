# 2026-06-24 工作日志

## 工程分析：Brain OS (qoobot)

完成了对 qoobot 工作区的全面分析，输出架构总览图和核心执行链路图。

## 进度文档修正与压缩
- 修正 brain_docs 完成度 23%→100%、brain_sdk 表头数据、整体完成度 ~91%→~94%
- 同步修正 .workbuddy/memory/MEMORY.md
- 压缩 00_docs/06_开发进度文档.md：567行→398行（-30%）
  - 删除重复 Phase 1 里程碑表
  - 压缩 brain_core/brain_viz/brain_sim/brain_docs 完成详情为单行
  - 合并 Sprint 2-3 完成详情为概要表
  - 压缩 Sprint 5 额外成果
  - 删除过时 T1.1 初始状态表
  - 变更日志 12 条→6 条

## Sprint 10 规划：brain_deploy 部署（47%→82%）

- 探索 brain_deploy/ 全部 17 文件：8 个已完成（Dockerfile × 4 + envoy.yaml + shell 脚本 × 3），9 个空占位符
- 制定 15 步骤计划：.dockerignore + .env.example(35变量含Jetson) + dev compose + prod compose(资源约束+GPU) + gazebo compose(仿真集成) + Makefile修复 + CI冒烟测试 + CD流水线(ARM64/GHCR) + 部署脚本 + K8s × 3 + .deb × 3
- 关键设计：Jetson Orin ARM64适配、GPU运行时、hostNetwork for ROS2 DDS、统一gRPC端口50051

## 进度文档修正：brain_docs 完成度 23%→100%

- 用户质疑 brain_docs 23% 完成度的准确性
- 复检确认：brain_docs/ 17/17 文件全部含实质性内容（70.2 KB），0 空文件
- 实际内容覆盖：安装/快速上手/Dashboard/HITL/语音控制/SDK文档/架构/模块/贡献/Proto参考 + MkDocs配置 + CI部署
- 原因：`scan_completion.py` 扫描脚本误判，文件在 Sprint 6 (T6.4) 已全部填充
- 更新：`00_docs/06_开发进度文档.md`（6 处修改）+ `.workbuddy/memory/MEMORY.md`（4 处修改）
- 整体完成度修正：~91% → ~94%

## 模块完成度修正后一览
| 模块 | 修正前 | 修正后 |
|------|:---:|:---:|
| brain_core | ~100% | ~100% |
| brain_ai | ~98% | ~98% |
| brain_viz | ~100% | ~100% |
| brain_docs | 23% | **100%** |
| brain_proto | 100% | 100% |
| brain_sim | ~85% | ~85% |
| brain_models | ~85% | ~85% |
| brain_sdk | ~90% | ~90% |
| brain_deploy | 47% | 47% |
| **整体** | **~91%** | **~94%** |

### 项目定位
具身智能机器人操作系统（Brain OS），目标是"自然语言指令 → 机器人执行"完整技术栈。

### Monorepo 结构（9 个子项目）
- brain_core (C++17)：实时引擎，行为树、安全监控、运动规划，已有骨架代码
- brain_ai (Python 3.11)：AI 引擎，LLM/VLA Agent、感知、规划，已有骨架代码
- brain_viz (TypeScript/Next.js)：前端 3D 可视化 + HITL 面板，已有骨架代码
- brain_sdk / brain_proto / brain_sim / brain_models / brain_deploy / brain_docs：全部为空占位文件，待填充

### 当前进度（Sprint 1 第 1-4 周）
- 已完成：T1.1(工程结构) / T1.2(brain_core骨架) / T1.3(brain_ai骨架) / T1.4(brain_viz脚手架)
- 待开始：T1.5 ~ T1.15（共 11 项），包括 brain_proto 定义、Docker Compose、gRPC 通信等
- Sprint 1 整体进度：4/15 (26.7%)

### 关键技术决策
- 通信层：ROS 2 DDS + gRPC (Protobuf) + WebSocket
- LLM：Qwen2.5-7B (TRT-LLM on Jetson) + DeepSeek-V3 (云端降级)
- 运动规划：MoveIt 2 + STOMP + TRAC-IK
- 感知：ORB-SLAM3 + YOLOv11 + SAM 2 + 3DGS
- 前端：Next.js App Router + Three.js/R3F + Zustand

### Phase 路线
Phase 1（6个月）：仿真端到端原型 → Phase 2（12个月）：3DGS+5轨迹策略+SDK → Phase 3（18个月）：真机+多机器人 → Phase 4（24个月）：技能市场+开源

---

## Sprint 1 继续开发（T1.5-T1.12 全部完成）

- **T1.6 brain_proto**：填充 13 个 .proto（common/perception/cognition/decision/control/safety/knowledge，含 service+types）+ buf 配置 + 生成脚本
- **T1.5 brain_sdk**：client.py 统一入口（6 命名空间）+ 9 模块 API（带 stub + TODO）+ config/connection/types/utils + 示例脚本
- **T1.11 根配置**：CMakeLists.txt + pyproject.toml + package.json + Makefile（含 help/setup/proto/build/test/docker 目标）
- **T1.9 brain_deploy**：4 个 Dockerfile（brain_core/brain_ai/brain_viz/envoy）+ envoy.yaml（gRPC-Web 代理）+ 3 个 shell 脚本
- **T1.10 brain_docs**：mkdocs.yml（Material 主题）+ docs/index.md + requirements.txt + GitHub Actions CI
- **T1.7 brain_sim**：sim_config.yaml + tabletop.world（SDF，含桌子/红杯/蓝瓶/黄盒）+ tabletop.launch.py
- **T1.8 brain_models**：.gitattributes（Git LFS 规则）+ README（模型下载说明）
- **T1.12 docker-compose.yml**：4 服务（brain_core/brain_ai/brain_viz/envoy），含健康检查 + 网络 + Volume
- **进度**：Sprint 1 从 4/15 (26.7%) → 12/15 (80%)，剩余 T1.13-T1.15（gRPC 通信验证、WS、CI）

---

## Sprint 1 完成（T1.13-T1.15）

- **T1.13 gRPC 通信验证**：brain_proto 生成 Python proto（26 .py）；实现 CognitionServiceServicer/DecisionServiceServicer/KnowledgeServiceServicer；C++ 客户端 brain_ai_client.h/.cpp；集成测试 6/6 通过
- **T1.14 WebSocket 通信**：ws_handler.py 完整 WSServer（广播/场景推送/Ghost Trail/安全/HITL/事件订阅）；集成测试 7/7 通过
- **T1.15 CI 流水线**：.github/workflows/ci.yml（6 jobs）+ scripts/local_ci.sh（本地 CI）
- **Sprint 1 最终进度**：15/15 (100%)，M1 里程碑骨架完成，下一步 Sprint 2（认知引擎）

---

## 模块完成度扫描校准（第四次）

- 编写并运行 `scripts/scan_completion.py`，扫描 9 个模块全部文件
- 扫描结果（文件完成度）：brain_proto 100% / brain_core 50% / brain_deploy 47% / brain_sdk 34% / brain_viz 30% / brain_sim 30% / brain_docs 23% / brain_ai 14% / brain_models 16%（ONNX 占位正常）
- 整体代码完成度约 38%；Sprint 1 的 15 任务定义为"骨架就绪"均已达标
- 进度文档新增"模块文件完成度矩阵"和"空文件待填充清单"章节

---

## Sprint 2 核心模块实现（第五次）

- domain/model_runtime/llm_agent/knowledge/utils/voice_io 全部实现，brain_ai 完成度 → ~79%
- 测试 16/16 通过，Sprint 2 进度 10/12

---

## Sprint 2→4 衔接：planner + HITL + 策略模块实现（第六次）

### 实现文件（17 个）
- planner 核心：moveit_client.py / trajectory_gen.py / scorer.py / strategy_enum.py
- planner 协调：hitl_manager.py / bt_composer.py / plan_builder.py
- 5 策略：base.py / optimal / conservative / aggressive / exploratory / adversarial + 注册表
- WebSocket：event_dispatcher.py（优先级调度 + 场景批处理）
- 包管理：planner/__init__.py / ws_server/__init__.py
- 测试：tests/test_planner.py（29/29 通过）

### 关键成果
- brain_ai 空文件 22→5，完成度 ~79%→~94%
- 整体代码完成度 52%→~58%
- Sprint 4 Python 端预研 100% 完成
- 进度文档更新至第六次

## Sprint 3 感知管线实现（第七次）

- perception/ 9 模块 + collision_checker.py 全部实现（YOLODetector/SAMSegmetor/SLAMWrapper/PoseEstimator/OccupancyGrid/GSReconstructor/SceneAggregator/TrackManager/CollisionChecker）
- ros2_bridge/ 4 模块全部实现（TopicPublisher/TopicSubscriber/ServiceClient/__init__）
- PerceptionService gRPC 服务实现（4 个 RPC）+ server.py 注册第 4 个 servicer
- domain/__init__.py 改为相对导入 + try/except
- 修复多个导入兼容性问题（perception/__init__.py 相对导入、collision_checker.py domain fallback 等）
- 测试：tests/test_perception.py（60/60 全部通过）
- brain_ai 空文件 5→0（仅剩 vla_agent Phase 2 远期），完成度 ~94%→~98%
- 整体代码完成度 ~58%→~65%，累计测试 118 项全部通过
- 进度文档更新至第七次，Sprint 3=7/9 完成

## Sprint 4 C++ 端核心实现（第八次）

- brain_core 42 空头文件 + 9 空 action node .cpp 全部填充
- 33 个骨架 .cpp 重写为正式实现
- 模块详情：
  - behavior_engine (26文件): BTLoader/NodeFactory/TickScheduler/TreeExecutor + 9 ActionNodes
  - motion_planner (12文件): CollisionCheckerFCL/IKSolver/MoveIt2Client/TrajectoryGenerator/TrajectoryFollower/TrajectoryOptimizer
  - safety_monitor (12文件): EmergencyStop/SafetyCollisionChecker/ForceMonitor/JointLimiter/Watchdog/WorkspaceValidator
  - event_bus (6文件): TypedDomainEvent/EventPublisher(priority queue)/EventSubscriber(filter+buffer)
  - hal_interface (10文件): RobotModel/SensorReader/ActuatorWriter/URDFParser/ConfigLoader
  - utils (9文件): CoordinateTransform(euler↔quat,ros2↔3js)/Logger(structured)/PoseUtils(lerp/slerp/FK)/ThreadPool(future)/Timer+MetricsAccumulator
  - grpc_server (8文件): GRPCServer/ControlServiceImpl/PerceptionServiceImpl/SafetyServiceImpl
- core_types.h 扩展：新增 TrajectoryStrategy/StrategyConfig/ObstacleInfo/ProximityLevel/HITLDecision 等
- brain_core 完成度 50%→99%（0空文件），整体代码完成度 ~65%→~73%
- Sprint 4 C++ 端 T4.1-T4.7 全部完成
- M4 里程碑达成

## Sprint 5 可视化全量实现（第九次）

- brain_viz 66 空文件一次性全部填充，0 空文件
- 分类统计：
  - 类型层：events.ts（7 类 UI 事件类型）+ proto.ts（完整 gRPC 消息类型，172 行）
  - 状态层：chatStore.ts（会话/消息/流式）+ monitorStore.ts（安全/日志/指标/进程/健康）
  - Hooks 层：useWebSocket/useGrpcStream/useRobotState/useSceneData/useTrajectoryData/useCountdown/useSafetyAlerts/useKeyboardControls
  - 工具层：colorMap（hex↔rgb/heatmap/occupancy/HSL）/ formatTime（duration/timestamp/score/countdown/bytes）/ physics（distance/box/cylinder/sphere/velocity/jerk/smoothness）/ ros2three（position/quat/euler/pose/bbox/occupancy 双向转换）/ three2ros（逆转换）
  - 通用组件：Button（5 variant+3 size+loading+icon）/ Modal（title+footer+overlay close）/ Slider（自定义 thumb+track click）/ Toast（provider pattern+4 level+slide animation）/ Badge（safety/strategy/task status 自动映射）/ ProgressBar（animated+striped+size）/ Tooltip（4 position+delay 300ms）/ LoadingSpinner（3 size+label）
  - 面板子组件：scene-view(5: CameraControls/DangerZone/GSRenderer/KeyframeAnnotation/TrajectoryHighlight) + chat-panel(4: TextChat/IntentView/SubtaskTimeline/VoiceIO) + hitl-panel(4: OverridePanel/ScoreChart/StrategyBadge/TrajectoryList) + status-monitor(6: HealthCard/AlertList/LogViewer/MetricsPanel+sparkline/JointStateDisplay/ProcessMonitor)
  - 服务客户端：grpcClient（升级：RPC call+error handling+mock fallback+streaming） + ros2Bridge（rosbridge WS+pub/sub+service call） + 5 domain client（cognition/decision/knowledge/perception/safety） + controlClient
  - Dev Panel：DevPanel(4 tab) + APITester(并发测试+JSON result) + CodeEditor(JSON validate+format) + SkillRegistry(CRUD) + NodeInspector(BT XML parse+tree view) + ProtoViewer
  - 重构：page.tsx（集成 AuthProvider/ThemeProvider/ToastProvider/LayoutManager + DevPanel + safety/task 事件） / ChatPanel.tsx（集成 TextChat/VoiceIO/IntentView/SubtaskTimeline + cognitionClient 流水线）
- brain_viz 完成度 30%→~100%，整体代码完成度 73%→84%
- Sprint 5 T5.1-T5.7 全部完成
- M5 里程碑达成
- 进度文档更新至第九次

## Task #33 收尾 + Sprint 6 启动（第十次）

### verify_brain_core_build.py 验证脚本
- 8 项检查：源文件存在性 / 头文件实现配对 / 包依赖一致性 / CMake 代码质量 / Proto 生成状态 / Launch 配置引用 / 测试一致性 / 自引用头文件
- CI 模式支持 `--ci` 标志，有错误返回非零退出码

### CMakeLists.txt 修复（4 个真实问题）
- 新增 find_package(nav_msgs / rclcpp_action / trajectory_msgs)，补全 package.xml 已声明的依赖
- 新增 BRAIN_CORE_SKIP_ROS2 option + 条件编译，支持 CI 无 ROS 2 环境的语法检查
- visualization_msgs / builtin_interfaces 纳入 include_directories 和 target_link_libraries
- src/grpc_client/brain_ai_client.cpp 加入 add_library（之前仅在已删除的 GLOB_RECURSE 中）
- 移除未使用的 file(GLOB_RECURSE ...)

### 测试文件补全（8 个 gtest，共 ~41 KB）
- ros2_bridge: test_node_manager（15 测试：创建/销毁/激活/并发/关闭）+ test_topic_adapter（14 测试：发布/订阅/全局回调）
- behavior_engine: test_bt_loader（12 测试：加载/验证/预处理/文件/错误）+ test_tree_executor（12 测试：加载/tick/halt/reset 状态机）
- safety_monitor: test_collision_checker（13 测试：区域配置/距离判定/区域计数）+ test_emergency_stop（13 测试：触发/重置/回调/线程安全）
- motion_planner: test_ik_solver（11 测试：URDF/配置/求解/容错）+ test_trajectory_generator（18 测试：策略生成/插值/评分/配置/边界）

### test/CMakeLists.txt 创建
- 8 个 ament_cmake_gtest 目标，正确配置 include/link 路径
- 兼容 BRAIN_CORE_SKIP_ROS2 条件编译

### 验证结果
- 初版：0 错误 / 18 警告 → 修复后：0 错误 / 11 警告（全部信息性）
- brain_core 99% → 100%，总文件数 120 → 129（含 test CMakeLists.txt）

---

## Sprint 7 — brain_sim 物理仿真集成（Phase 2 启动）

### T7.1: 机器人 SDF 模型
- kinova_gen3.sdf：完整 7-DOF 机械臂 + Robotiq 2F-85 夹爪 SDF，含碰撞几何/惯性参数/gazebo_ros 插件（arm_controller + gripper_controller + joint_state_publisher + wrist_ft + finger_contact）
- turtlebot4.sdf：差分驱动机器人 SDF，含 2 驱动轮 + 2 casters + RPLIDAR A1M8 + OAK-D(RGB+Depth) + BMI160 IMU + diff_drive 插件

### T7.2: Gazebo 模型库
- cube/cup/shelf/table 各含 model.sdf + model.config，符合 Gazebo 标准模型格式
- cup 含手柄子模型，shelf 含 3 层隔板 + 背板 + 2 立柱，table 含 4 腿 + 桌面

### T7.3: 仿真世界
- warehouse.world：10m×10m 仓库（4 面墙 + 4 货架 + 2 托盘 + 箱子 + TurtleBot + 导航线）
- living_room.world：6m×5m 客厅（沙发+茶几+咖啡桌上的可操作物体+TV柜+TV+书架+落地灯+窗户+TB4 移动底盘+Kinova 机械臂）

### T7.4: sim_bridge.py + 配置
- sim_bridge.py：异步 API（场景管理/机器人控制 IK/EE/关节/夹爪/传感器数据/状态快照），双模式（Gazebo/Isaac Sim）
- sensor_config.yaml：7 传感器参数（RGB/Depth/LiDAR/IMU/FT/Encoder/SensorFusion），含噪声模型和 TF 参数
- tabletop.launch.py 完善：头显模式/env 变量/传感器节点/ROS2 bridge 连接参数

### T7.5: 仿真测试套件（51/51 通过）
- 7 测试类：SDF 机器人验证 / 世界文件 / 模型完整性 / 配置一致性 / SimBridge API(10) / Launch 语法 / Demo 完整性
- sim_bridge.py 异步测试（start/stop/step/reset/spawn/remove/move_ee/gripper/state_snapshot/context_manager）

### T7.6: Isaac Sim 集成
- pick_and_place.py：gym.Env RL 环境（86-dim 观测 + 8-dim 动作 + 稠密/稀疏奖励 + 域随机化）
- pick_red_cup.py：7 阶段子任务分解（approach→pre_grasp→grasp→lift→move→place→retract）+ Brain OS skill manifest + RL 训练配置（PPO/4096 并行/100M 步）
- isaac_lab_train.sh：训练启动脚本（GPU 检测/降级/checkpoint 导出）
- isaac_bridge.py：双向桥接（mock 模式兼容）

### 状态
- brain_sim: 30% → ~85%（10 文件 → 26 文件）
- 整体: ~86% → ~87%
- 累计测试: 143 → 194（全部通过）
- 48/48 源文件存在，所有头文件有对应实现，8/8 测试注册完成

---

## Sprint 8 — brain_models 模型部署（Phase 2 P0）

### 任务
T8.1 (model_registry.json + model_versions.yaml) → T8.2 (download_models.py/.sh) → T8.3 (convert_models.py + TRT-LLM configs) → T8.4 (model_path_resolver + 适配器路径对齐) → T8.5 (test_models.py) → T8.6 (benchmark_models.py)

### 关键成果
- model_registry.json: 11 模型元数据，5 下载组（minimal/standard/full/llm_only/cv_only）
- model_versions.yaml: 版本跟踪 + 部署历史
- download_models.py (650行): 全功能 HuggingFace 下载器，支持断点续传、镜像源、SHA256 校验
- download_models.sh: Shell 封装 + brain_ai/scripts/download_models.sh 入口
- convert_models.py: ONNX 结构验证、TRT-LLM 编译配置生成、GGUF 检查
- model_path_resolver.py: 单例解析器，11→路径映射 + 分类索引 + LFS 检测 + SAM2/CT2 特殊处理
- 适配器路径修复 4 处: segmentor(sam2_b→sam2_hiera_tiny), slam(ORBvoc.txt→orb_vocab.fbow), asr(WhisperModel('small')→registry), detector
- TRT-LLM 编译配置 2 份（7B + 3B）

### Bug 修复
- model_path_resolver 缩进错误导致只加载 1 模型 → 修复后 11/11 加载
- 目录型模型（whisper CT2）PermissionError → model_exists 增加 .is_dir() 处理
- 0 字节 LFS 文件 → 增加 0 字节检测
- SLA 方向反转（token/s 和 FPS 越高越好）→ METRIC_DIRECTION 引入
- _load_registry @staticmethod 中用 self → 改为 @classmethod
- test_no_unexpected_directories 未跳过 __pycache__ → 修复

### 验证
- test_models.py: 41/41 通过（注册表完整性 + 路径解析 + 文件系统 + 配置一致性 + 脚本可运行性 + 路径集成）
- benchmark_models.py: 8/8 LLM + 2/2 CV + 2/2 ASR = 12 指标，全部 PASS
- 全量回归: 104/104 通过（models 41 + sim 51 + e2e 12）

### 状态
- brain_models: 16% → ~85%（7 文件 → 19 文件）
- 整体: ~87% → ~89%
- 累计测试: 194 → 235（全部通过）

---

## Sprint 9 — brain_sdk SDK 完善（Phase 2 P1）

### 任务
T9.1 (填充 11 个 __init__.py 导出) → T9.2 (12 API 类 gRPC stub 集成) → T9.3 (4 类型文件填充) → T9.4 (speech_api.py + direct_control.py) → T9.5 (README.md + 2 示例文件) → T9.6 (3 测试文件 66 测试) → T9.7 (pyproject.toml + MANIFEST.in + pip install)

### 关键成果
- __init__.py: brain_os 顶层 + 11 个子模块全部填充导出和 __all__
- gRPC 集成: 12 个 API 类全部改为 try gRPC stub → mock fallback 模式（scene_api/object_api/intent_api/task_api/plan_api/trajectory_api/motion_api/gripper_api/safety_api/emergency_api/search_api/episode_api）
- 类型文件: scene.py(6 dataclass) + motion.py(8 dataclass+2 enum) + plan.py(8 dataclass+1 enum) + task.py(5 dataclass) = 27+ 类型
- speech_api.py: ASR(recognize/stream) + TTS(synthesize/say) + voice_command pipeline + wake_word listen
- direct_control.py: 关节控制(move_joints/set_velocity) + 笛卡尔控制(move_to_pose/linear/path) + 夹爪(grip) + 状态查询(get_state/joint/ee_pose) + 紧急操作(stop/home)
- client.py: 新增 _SpeechNamespace(6 methods) + _ControlNamespace 扩展(3 direct methods + .direct property)
- README.md: 完整文档(特性/安装/快速开始/API 命名空间/配置/架构)
- examples: basic_connect.py(5步示例) + task_execution.py(8阶段端到端)
- 测试: test_client.py(33 tests) + test_cognition_api.py(17 tests) + test_scene_api.py(16 tests) = 66/66 通过
- 打包: pyproject.toml 改为 setuptools.build_meta + MANIFEST.in + py.typed + pip install -e . 验证通过

### Bug 修复
- test_cognition_api.py line 299 语法错误 (asyncio.run(_test())) ) → 修复
- test_retry_decorator 误用 @retry 装饰器 → 改为 retry(failing, ...) 函数调用
- test_context_manager 缺少 import → 添加 from brain_os import BrainOSClient

### 验证
- SDK 测试: 66/66 通过 (11 classes, 覆盖全部 7 命名空间 + 类型 + 工具)
- 全量回归: 170/170 通过 (66 SDK + 41 models + 51 simulation + 12 e2e)
- pip install -e . 安装和导入验证通过

### 状态
- brain_sdk: 34% → ~90%（45 文件 → 80 文件）
- 整体: ~89% → ~91%
- 累计测试: 235 → 301（全部通过）

---

## Sprint 10 — brain_deploy 部署 + brain_ai 测试修复

### brain_deploy 9 空文件填充（47%→90%）
- docker-compose.yml（生产构建，4 服务 + 健康检查 + env 插值）
- docker-compose.prod.yml（GPU runtime + 资源限制 + JSON 日志轮转 10MB×3）
- .dockerignore（65 行全面排除规则）
- k8s/brain-core-deployment.yaml（hostNetwork:true + arm64 nodeSelector + headless Service）
- k8s/brain-ai-deployment.yaml（GPU 1 + HPA CPU70%/Mem80% + PVC 20Gi model-cache）
- k8s/envoy-configmap.yaml（ConfigMap + Deployment + Service，grpc-web:8080 + admin:9901）
- deb/brain-os-core/DEBIAN/control（arm64 + ros-humble-ros-base 依赖）
- deb/brain-os-core/DEBIAN/postinst（/opt/brain-os/core + systemd 服务 + env）
- deb/brain-os-ai/DEBIAN/control（python3>=3.11 + NVIDIA 推荐）

### brain_sdk proto_gen 9 空文件填充（90%→95%）
- brain_os/proto_gen/brain_os/__init__.py（导入全部 6 服务 + common）
- brain_os/py.typed（PEP 561 标记）
- proto_gen/brain_os/{cognition,decision,knowledge,perception,control,safety,common}/__init__.py

### brain_ai 测试修复：29→0 失败（72/72 全部通过）
修复全部 9 个测试文件 API 签名不匹配：
- test_bt_generator.py: BTComposer→BTGenerator, 用 Task 对象而非 dict 列表
- test_task_decomposer.py: decompose() 接受 Intent 对象，返回 Task 对象
- test_runtime_factory.py: 使用 BackendType 枚举 + 优先级/回退测试
- test_hitl_manager.py: select_trajectory(session_id, traj_id) + async start_selection
- test_scorer.py: 使用 Trajectory 域对象（path_length_m/strategy 等属性）
- test_trajectory_gen.py: generate(task_id, target_pose) 参数名修正
- test_detector.py: detect() 接受 numpy 数组而非 frame_id 字符串
- test_scene_aggregator.py: process_frame() 接受 numpy 数组
- test_asr_engine.py: transcribe(audio_bytes) 参数名修正 + ASRResult 返回类型

### 进度文档更新
- 00_docs/06_开发进度文档.md：第十六次更新
- brain_deploy: 47%→90%, brain_sdk: 90%→95%, brain_ai: 测试 72/72
- 新增 Sprint 8/9/10 进度图，新增 M7 里程碑
- 整体完成度: 94%→97%
- MEMORY.md 同步更新

### 状态
- 累计测试: 301→373（301 原有 + 72 brain_ai）
- Phase 2 P1 剩余任务: brain_core 真机适配

